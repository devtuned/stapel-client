import { AppConfiguration } from './env.interface'

/*
 * This file will be replaced during build by using the `fileReplacements` array
 * The list of file replacements can be found in `angular.json`
 *
 * `ng build --prod` replaces `.env.ts` with `production.env.ts`
 */

export declare const appConfiguration: AppConfiguration
